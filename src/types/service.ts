export type Service = {
  imgURL: string;
  label: string;
  subtext: string;
};

export type Review = {
  imgURL: string;
  customerName: string;
  rating: number;
  feedback: string;
};

export type Shoe = {
  thumbnail: string;
  bigShoe: string;
};
